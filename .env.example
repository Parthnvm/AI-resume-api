SECRET_KEY=your-secret-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# --- Firebase Auth (for Login / Register / Forgot Password) ---
# Firebase Console → Project Settings → General → Web API Key
FIREBASE_API_KEY=your-firebase-web-api-key

# --- Production Settings (set on Render Dashboard) ---
# FLASK_ENV=production
# DATABASE_URL=sqlite:///resume_shortlister.db  (or a PostgreSQL URL)
# UPLOAD_FOLDER=/opt/render/project/src/resumes  (Render persistent disk path)
